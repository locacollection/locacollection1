window.LOCA=window.LOCA||{};function cartEntries(){return Object.entries(LOCA.cart||{}).filter(([,q])=>Number(q)>0)}function cartTotal(){return cartEntries().reduce((s,[id,q])=>{const p=LOCA.products.find(x=>String(x.id)===String(id));return s+(p?p.price*q:0)},0)}
async function openCheckout(){if(!cartEntries().length){alert('Your bag is empty.');return}if(!LOCA.currentUser){openAuth('signup');alert('Create your LOCA account or sign in before checkout.');return}await LOCA.ensureProfile();const p=LOCA.profile||{};document.getElementById('coName').value=p.full_name||LOCA.currentUser.user_metadata?.full_name||'';document.getElementById('coEmail').value=LOCA.currentUser.email||'';document.getElementById('coPhone').value=p.phone||'';document.getElementById('coCity').value=p.city||'';document.getElementById('coAddress').value=p.address||'';document.getElementById('checkoutWelcome').textContent='Signed in as '+(p.full_name||LOCA.currentUser.email);document.getElementById('coTotal').textContent=LOCA.money(cartTotal());document.getElementById('checkoutModal').classList.add('open');document.body.classList.add('lock');setTimeout(()=>document.getElementById('coPhone')?.focus(),80)}
function closeCheckout(){document.getElementById('checkoutModal')?.classList.remove('open');if(!document.querySelector('.modal-layer.open,.bag-drawer.open'))document.body.classList.remove('lock')}
async function placeOrder(e){e.preventDefault();if(!LOCA.currentUser){closeCheckout();openAuth('signup');return}const form=e.target,button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Placing order...';try{const items=cartEntries().map(([id,quantity])=>({product_id:Number(id),quantity:Number(quantity)}));const{data,error}=await LOCA.db.rpc('place_order',{p_name:document.getElementById('coName').value.trim(),p_phone:document.getElementById('coPhone').value.trim(),p_email:LOCA.currentUser.email||'',p_address:document.getElementById('coAddress').value.trim(),p_city:document.getElementById('coCity').value.trim(),p_payment:document.getElementById('coPayment').value,p_items:items});if(error)throw error;const result=Array.isArray(data)?data[0]:data;await clearCart();closeCheckout();document.getElementById('drawer')?.classList.remove('open');document.getElementById('overlay')?.classList.remove('open');alert('Order confirmed: '+(result?.order_number||'LOCA-ORDER')+'\n\nWe will contact you on the phone number provided.');form.reset()}catch(err){alert(err.message||'Order could not be placed.')}finally{button.disabled=false;button.textContent='Place order ↗'}}
async function loadMyOrders(){const box=document.getElementById('myOrders');if(!box||!LOCA.currentUser)return;box.innerHTML='<div class="account-loading">Loading orders...</div>';const{data,error}=await LOCA.db.from('orders').select('*').eq('user_id',LOCA.currentUser.id).order('created_at',{ascending:false});if(error||!data?.length){box.innerHTML='<div class="orders-empty">No orders yet.</div>';return}box.innerHTML=data.map(o=>`<article class="order-card"><div><strong>${o.order_number||o.id}</strong><span>${new Date(o.created_at).toLocaleDateString('en-PK')}</span></div><div class="order-card-right"><b>${LOCA.money(o.total)}</b><span>${o.status||'Pending'}</span></div></article>`).join('')}
window.openCheckout=openCheckout;window.closeCheckout=closeCheckout;window.placeOrder=placeOrder;window.loadMyOrders=loadMyOrders;